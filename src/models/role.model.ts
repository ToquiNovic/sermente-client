export interface Role {
  id: string;
  name: string;
  state: boolean;
  description: string;
}

export interface RolePayload {
  id: string;
  name: string;
  state: boolean;
  description: string;
}